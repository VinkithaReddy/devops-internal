Git commands 

git init

git add .

git commit -m "Initial commit"

git remote add origin https://github.com/VASU-DUBBANABOINA/devops.git

git branch -M main

git pull origin main --rebase

git push -u origin main


docker login 
docker pull hello-world
docker run hello world
docker pull ubuntu
docker run ubuntu
docker ps -a
docker images
docker start containerId
docker stop containerId
docker remove containerId
docker tag ubuntu:latest vinkitha/ubuntu:latest
docker run vinkitha
